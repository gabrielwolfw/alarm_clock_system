#include <stdio.h>
#include "timer.h"

int main() {
    // Inicializa el temporizador y configura las interrupciones
    init_timer();

    while (1) {
        // Bucle infinito para mantener el programa en ejecución
        // Aquí puedes agregar código adicional para manejar otras tareas,
        // como leer la configuración de la alarma desde botones o actualizar el display
    }

    return 0;
}

